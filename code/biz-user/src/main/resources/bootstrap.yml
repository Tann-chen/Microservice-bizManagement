server:
  port: 9001

spring:
  application:
    name: biz-user
  cloud:
    config:
      uri: http://config:8899
      fail-fast: true
      username: devAdmin
      password: erp!@#ERP

eureka:
  instance:
    hostname: biz-user
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://registry:8761/eureka/

