server:
  port: 9001

spring:
  application:
    name: com.user
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: devAdmin
      password: erp!@#ERP

eureka:
  instance:
    hostname: com.user
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/