server:
  port: 8761

spring:
  application:
      name: registry
  cloud:
      config:
        uri: http://localhost:8899
        fail-fast: true
        username: devAdmin
        password: erp!@#ERP

eureka:
  instance:
      hostname: localhost
  client:
      registerWithEureka: false
      fetchRegistry: false
      serviceUrl:
          defaultZone: http://localhost:${server.port}/eureka/