server:
  port: 8761

spring:
  application:
      name: registry
  cloud:
      config:
        uri: http://localhost:8888
        fail-fast: true
        username: devAdmin
        password: erp!@#ERP

eureka:
  instance:
      hostname: registry
  client:
      registerWithEureka: false
      fetchRegistry: false
      serviceUrl:
          defaultZone: http://localhost:${server.port}/eureka/