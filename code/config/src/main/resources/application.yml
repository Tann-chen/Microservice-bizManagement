server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Tann-chen/microservice-erp-configs
      label: master

security:
  com.user:
    name: devAdmin
    password: erp!@#ERP